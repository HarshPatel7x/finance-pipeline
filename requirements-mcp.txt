# MCP server deps (local dev tooling) — NOT part of the Lambda bundle.
# deploy.sh packages only requirements.txt; this file is installed separately:
#   pip install -r requirements-mcp.txt
# Full pinned freeze of the delta from 'pip install mcp==1.27.2' into the
# requirements.txt venv (python3.12), so pip-audit --no-deps covers transitives.
attrs==26.1.0
cffi==2.0.0
click==8.4.1
cryptography==48.0.1
httpx-sse==0.4.3
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
mcp==1.27.2
pycparser==3.0
pydantic-settings==2.14.1
PyJWT==2.13.0
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.5.1
sse-starlette==3.4.4
starlette==1.3.0
uvicorn==0.49.0
